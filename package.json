{
  "name": "swedish-geo-history",
  "scripts": {
    "backend": "cd backend-api && dotnet run",
    "frontend": "cd services && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "maplibre-gl": "^5.16.0"
  }
}
